# 🏠 HiTE PRO Gateway — API Тесты

Автоматизированный набор тестов для проверки корректности работы REST API шлюза умного дома **HiTE PRO Gateway**.  
Тесты написаны на **JavaScript** с использованием фреймворка **Playwright** в режиме **API-only** (без браузера).

---

## ✅ Что тестируется

### 📡 Чтение состояния устройств
Поддержка всех типов устройств из официальной спецификации:

| Тип устройства           | Ожидаемый формат `status`                   | Диапазон / значения                                        |
|--------------------------|---------------------------------------------|------------------------------------------------------------|
| `switch`                 | `boolean`                                   | `true` / `false`                                           |
| `motion`                 | `boolean`                                   | `true` / `false`                                           |
| `dimmer`, `illumination` | `integer`                                   | `0–100`                                                    |
| `drive`                  | `integer`                                   | `0` – Открыт, `1` – Закрыт, `2` – Открытие, `3` – Закрытие |
| `temperature`            | `float`                                     | `-40 … +50`                                                |
| `humidity`               | `integer`                                   | `0–100`                                                    |
| `checker`                | `integer`                                   | `0` – Закрыто, `1` – Открыто                               |
| `water`                  | `integer`                                   | `0` – Норма, `1` – Залив                                   |
| `power`                  | `integer`                                   | `0` – Нет напряжения, `1` – Есть                           |
| `LED`, `LED3S/M`, `RGBW` | `integer` (яркость) + `string` (цвет в HEX) | Яркость: `0–100`, Цвет: `#ff0000`                          |

> 💡 Устройства без обратной связи (`Gateway ready`) не возвращают `status` в списке и дают `404` при запросе `/devices/{id}` — это также учитывается.

### ⚙️ Управление устройствами
- Включение/выключение реле (`switch`)
- Установка яркости диммера (`dimmer`)
- Управление приводом (`drive`: открыть/закрыть/остановить)
- Управление цветом RGBW-устройств через query-параметры:
  - `color=ff5733`
  - `colorTemperature=4000`
  - `effect=2`

### 📋 Дополнительно
- Получение списка устройств и сценариев
- Чтение логов устройств и сценариев
- Обработка ошибок (`401`, `404`, `500`)

---

## 🛠️ Требования

- **Node.js** версии 18 или новее
- Доступ к шлюзу **HiTE PRO Gateway** по локальной сети или через внешний URI
- Учётная запись с правами управления (логин/пароль)

---
